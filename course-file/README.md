This course-file contains course material for future reference. 
I implemented a Bash script that automates daily backups by detecting files modified within the last 24 hours using Unix timestamps, arrays, and tar-based compression.
